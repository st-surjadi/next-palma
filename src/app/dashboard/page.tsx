import { Metadata } from "next";
import { FC } from "react";

export const metadata: Metadata = {
  title: 'Dashboard',
}

const Page: FC = () => {
  return (
    <div>
      Dashboard Page
    </div>
  )
}

export default Page;
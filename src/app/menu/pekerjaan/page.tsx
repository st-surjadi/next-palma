import { Metadata } from 'next';
import { FC } from 'react';

export const metadata: Metadata = {
  title: 'Pekerjaan',
};

const Page: FC = () => {
  return <div>Pekerjaan</div>;
};

export default Page;

import { Metadata } from "next";
import { FC } from "react";

export const metadata: Metadata = {
  title: "Menu",
};

const Page: FC = () => {
  return <div>Menu Page</div>;
};

export default Page;
